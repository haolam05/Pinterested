<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#" style="padding-left: 22px">PINTERESTED</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="nav navbar-nav navbar-collapse add_right_padding" style="display: flex; justify-content: flex-end;">

      <li class="nav-item active navbar-text add_right_padding">
        <%= link_to 'Home', root_path %>
      </li>
      <li class="nav-item active navbar-text add_right_padding">
        <%= link_to 'About Me', home_about_path %>
      </li>
      
      <% if user_signed_in? %>
        <li class="nav-item active navbar-text add_right_padding">
          <%= link_to 'Edit Profile', edit_user_registration_path %>
        </li>
        <li class="nav-item active navbar-text add_right_padding">
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
        </li>
      <% else %>
        <li class="nav-item active navbar-text add_right_padding">
          <%= link_to 'Login', new_user_session_path %>
        </li>
        <li class="nav-item active navbar-text add_right_padding">
          <%= link_to 'Join', new_user_registration_path %>
       </li>
      <% end %>
    </ul>
  </div>
</nav>
